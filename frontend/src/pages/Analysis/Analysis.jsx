/**
 * Componente da Rota de Análise (/analisar).
 * Funciona como um alias para a página Home, reaproveitando a interface principal
 * de análise de conteúdo técnico.
 */

import Home from "../Home/Home";

function Analysis() {
  return <Home />;
}

export default Analysis;